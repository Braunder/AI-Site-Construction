# AI Site Construction (MVP) — TODO

## Фаза 1: Инфраструктура и UI формы

### 1.1 Развёртывание FastAPI
- [x] Структура проекта
- [x] poetry/pip зависимости (`requirements.txt`, pip + venv)
- [x] env-переменные (`pydantic-settings`, `.env`)
- [x] Логирование (консоль + `logs/app.log`, access-log middleware)
- [x] CORS
- [x] Базовые роуты
- [x] Middleware (CORS, Session, access-log)

### 1.2 Модели данных и админка
- [x] SQLAlchemy модели: Project, History
- [x] Alembic миграции (`alembic/versions/0001_initial.py`)
- [x] SQLAdmin для просмотра данных (`/admin`)

### 1.3 Вёрстка формы генерации
- [x] Jinja2 + Bootstrap 5
- [x] Поля: выбор шрифта (или семейства)
- [x] Color pickers: основной, акцентный, фоновый цвета
- [x] Селектор стилевого направления (минимализм, брутализм, корпоративный и т.д.)
- [x] Textarea для текстового описания желаемого сайта
- [x] Drag-and-drop зона загрузки референсного изображения
- [x] Адаптивный дизайн
- [x] Валидация полей перед отправкой

### 2.1 Загрузка и подготовка изображений
- [x] Обработка multipart-загрузки
- [x] Валидация формата/размера изображения (JPG/PNG, до 5 МБ, проверка magic bytes)
- [x] Сохранение на диск (`uploads/`)
- [x] Подготовка base64 для OpenAI Vision (data URL)

---

## Фаза 2: LLM-интеграция, генерация и редактирование

### 3.1 Интеграция OpenAI API
- [x] HTTP-клиент (httpx, async)
- [x] System-промпт для генерации HTML/CSS/JS
- [x] Передача изображения base64 в vision
- [x] Обработка ответа LLM
- [x] Retry-логика (транспортные ошибки, 5xx, невалидный HTML)
- [x] Обработка ошибок API (статус проекта error + сообщение)

### 3.2 Парсинг и валидация кода
- [x] Извлечение HTML из markdown-блоков
- [x] Проверка структуры (DOCTYPE, html, body)
- [x] Базовая санитизация (см. 4.2)
- [x] Сохранение в БД

### 3.3 Модуль предпросмотра
- [x] Sandbox-iframe для рендера HTML (`sandbox="allow-scripts"`)
- [x] Изоляция (CSP `sandbox allow-scripts` на /preview, изолированный origin)
- [x] Корректное отображение inline-ресурсов

### 4.1 Чат-редактирование
- [x] UI чата (Jinja2)
- [x] Отправка текущего HTML + инструкции пользователя в LLM
- [x] Получение нового кода
- [x] Замена предыдущей версии
- [x] Сохранение цепочки в History

### 4.2 Валидация и безопасность
- [x] Автопроверка сгенерированного кода (`app/services/codegen.py`)
- [x] Проверка на XSS (javascript:-ссылки, data:text/html, meta refresh)
- [x] Проверка на запрещённые теги/скрипты (iframe/object/embed, внешние скрипты вне белого списка CDN; собственный санитайзер — bleach не применим к полным HTML-документам со `<style>`/`<script>`)
- [x] Механизм полной перегенерации (кнопка + random seed) при неудаче

---

## Фаза 3: История, UX-доработки и экспорт

### 5.1 Лента проектов
- [x] Страница со списком всех созданных проектов (UUID, дата, превью промпта)
- [x] Прямые ссылки на просмотр/редактирование
- [x] Удаление проекта

### 5.2 UX-улучшения формы
- [x] Пресеты стилей (кнопки «Лендинг», «Портфолио», «Визитка»)
- [x] Примеры промптов-подсказок
- [x] Индикаторы загрузки (оверлей + опрос статуса)
- [x] Финальная мобильная адаптация

### 6.1 Экспорт сайтов
- [x] Кнопка «Скачать .html» (чистый сайт)
- [x] Корректная кодировка UTF-8
- [x] Именование файлов (slug + id, RFC 5987)

### 6.2 Оптимизация промптов
- [x] Доработка system-промптов для стабильного вывода
- [x] Тесты на разные ниши (услуги — кофейня, товары — магазин чая, портфолио — UX-дизайнер; `scripts/e2e_niches.py`)
- [x] Проверка работы с разными референсными изображениями (PNG-референс в E2E)
- [x] Устранение типичных артефактов LLM (жёсткие правила вывода, автопроверка + перегенерация)

---

## Фаза 4: Тестирование, исправление и запуск

### 7.1 Функциональное тестирование
- [x] E2E проверка полного цикла: форма → генерация → предпросмотр → правка → скачивание (`scripts/e2e_http.py` — PASSED на локальной LLM)
- [x] Тесты на большие промпты (лимит 5000 символов, тест 422)
- [x] Тесты на некорректные изображения (тип, размер, magic bytes — `tests/test_images.py`, `tests/test_api.py`)
- [x] Тесты на отказ сети к OpenAI (retry + статус error — `test_llm_failure_sets_error_status`)
- [x] Граничные случаи (пустой промпт, невалидный цвет/стиль, 404, chat до генерации)
- [x] E2E-тестирование на локальной LLM (llama-server, Qwen3.5-9B @ 127.0.0.1:8080)

### 7.2 Исправление ошибок
- [x] Устранение багов вёрстки (двойной class у бейджа статуса)
- [x] Устранение багов iframe (sandbox + cache-bust при обновлении)
- [x] Устранение багов обработки ошибок API (Form-параметры, 409 при занятом проекте)
- [x] Корректность скачиваемых сайтов (UTF-8, RFC 5987 filename)
- [x] Edge cases в чате (блокировка ввода во время обработки, опрос статуса)

### 8.1 Деплой и передача
- [ ] Деплой на VPS/облако (по согласованию с заказчиком)
- [x] Передача исходного кода (репозиторий проекта)
- [x] Инструкция по запуску и настройке env (OPENAI_API_KEY) — `README.md`
- [x] Файл `.env.example` с перечнем необходимых переменных окружения для подключения LLM
- [x] Демонстрация работы сервиса на тестовой LLM (полный E2E на Qwen3.5-9B)
- [ ] Финальная настройка production-конфига и выбор модели (Заказчик самостоятельно)

---

## Фаза 5: Дополнительные доработки (сессия 2026-08-23)

### 9.1 Публичная главная и демонстрации
- [x] Главная страница `/` публична: hero, возможности, CTA (без генерации)
- [x] Отдельная страница демонстраций `/demos` (визитка, лендинг, портфолио)
- [x] Ссылка «Демонстрации» в публичной навигации

### 9.2 Авторизация и разграничение доступа
- [x] `/generate` и `/projects` доступны только после логина (redirect на /login)
- [x] Кнопка «⚙️ Админ» в шапке только для админов (не нужно вручную вводить /admin)
- [x] `/admin` защищён: sqladmin `AdminAuth.authenticate` проверяет user_id + is_admin
- [x] После входа пользователь попадает в личный кабинет `/projects`

### 9.3 Личный кабинет
- [x] Карточка пользователя: логин, роль, дата регистрации
- [x] Прогресс квоты генераций (прогресс-бар зелёный/жёлтый/красный)
- [x] Бейдж «♾️ Безлимит» для админов и unlimited-аккаунтов
- [x] Кнопка «Админ-панель» в кабинете (только для админов)

### 9.4 Исправление багов UI
- [x] Удаление проекта из списков: URL исправлен (`DELETE /api/projects/{id}` вместо несуществующего `/delete`)
- [x] Подключён `detail.js` на страницу проекта — заработали кнопки «Перегенерировать»/«Удалить»
- [x] Передача `window.PROJECT_STATUS` для опроса статуса при открытии страницы в работе
- [x] Footer: убрана ошибка `UndefinedError: 'now' is undefined`

### 9.5 Безопасность (по итогам breaker-отчёта)
- [x] **Critical:** XSS-обход через whitespace в схеме (`java\tscript:`) — добавлены паттерны с допусками `\t\n\r\x00-\x1f`
- [x] Inline style в одинарных кавычках с внутренними двойными — regex переписан, внешние CSS-ресурсы детектятся
- [x] iframe sandbox: убран `allow-same-origin` (остался только `allow-scripts`) — изоляция от parent DOM/cookies
- [x] Возврат квот (`refund_generation`) владельцам проектов, прерванных перезапуском сервера
- [x] Предупреждение в лог при дефолтном SECRET_KEY
- [x] Маскирование `llm_api_key` в деталях админки (ключ не отображается открытым текстом)
- [x] Шифрование ключей LLM-провайдеров в БД (`LLM_SECRETS_KEY` + миграция существующих значений)
- [x] Тарифная защита прямого upload ассетов и откат квоты/проекта при ошибке файлов
- [x] Тесты на новые векторы санитайзера (whitespace-схемы, смешанные кавычки) — `tests/test_codegen.py`
- [x] Блокировка inline event handlers (`on*=`) и CSS `url(javascript:/data:text/html)` в санитайзере
- [x] CSP-sandbox + nosniff при отдаче SVG/HTML ассетов (защита от stored XSS)
- [x] TTL 7 дней для подписанных file-токенов
- [x] Fail-fast при деривации ключа шифрования из дефолтного SECRET_KEY
- [x] Лок до списания квоты в chat/regenerate (409 не сжигает генерацию) + тест гонки
- [x] delete_element с учётом вложенности одноимённых тегов + тест
- [x] Refund при рестарте по каждому прерванному проекту; отказ от Windows reserved names; дедупликация чередующихся промптов; CSS url(data:/expression()) в санитайзере
- [x] Fail-fast на дефолтный SECRET_KEY (подделка сессии = админ); миграция шифрования не работает под публичным дефолтом
- [x] Маскирование тела ошибок LLM в error_message; TTL файловых токенов 1 час; CSP-sandbox для js/svg/html-ассетов; nosniff на /preview

### 9.6 Инфраструктура запуска
- [x] Виртуальное окружение `.venv`, зависимости из `requirements.txt`
- [x] Скрипты создания/удаления админа работают через `$env:PYTHONPATH="."` (PowerShell)

---

## Фаза 6: Полноценные сайты (многофайловый режим) — 2026-08-23

### 10.1 Хранение проектов нового поколения
- [x] Папка на пользователя: `data/sites/<user_id>/` — личное пространство
- [x] Папка на проект: `data/sites/<user_id>/<project_id>/` с `index.html` + ассеты
- [x] Сервис `app/services/sites.py`: сохранение/чтение/листинг/удаление файлов, лимиты (50 файлов, 50 МБ)
- [x] Модель `Project.is_multifile` + Alembic-миграция (`7a2f1c9d4e08`)
- [x] Защита от path traversal при отдаче файлов
- [x] Удаление папки проекта вместе с проектом

### 10.2 Загрузка пользовательских файлов в сайт
- [x] API `POST /api/projects/{id}/assets` — загрузка изображений (jpg/png/gif/webp/svg), шрифтов, css/js/json/csv
- [x] API `GET /api/projects/{id}/assets` — список файлов проекта
- [x] API `GET /api/projects/{id}/files/{name}` — отдача файла (для превью и сайта)
- [x] API `DELETE /api/projects/{id}/assets/{name}` — удаление файла
- [x] Множественная загрузка при создании проекта (`files[]` + флаг `multifile`)

### 10.3 LLM генерирует полноценные сайты
- [x] Промпт `MULTIFILE_EXTRA_PROMPT`: модель вставляет загруженные изображения по относительным путям из манифеста
- [x] Анимации и интерактив: CSS keyframes/scroll-reveal/hover, чистый JS (аккордеоны, табы, слайдеры, фильтры, валидация форм)
- [x] Динамический контент (каталоги, галереи) из встроенных JS-данных — без сервера
- [x] Манифест ассетов передаётся в промпт при генерации/перегенерации

### 10.4 Кастомные шрифты
- [x] Папка `data/fonts/` — общая библиотека шрифтов
- [x] Сервис `app/services/fonts.py`: валидация (woff2/woff/ttf/otf, до 5 МБ), @font-face генератор
- [x] API `/api/fonts`: список (все пользователи), загрузка/удаление (только админы), отдача файлов
- [x] UI-страница `/fonts`: библиотека со списком, загрузка/удаление (админ), ссылка в меню

### 10.5 Backlog фазы 6 — выполнено (2026-08-23)
- [x] UI на странице проекта: менеджер файлов (загрузка, список с типами, просмотр, удаление)
- [x] Чекбокс «🧩 Полноценный сайт» в форме генерации + зона множественной загрузки файлов
- [x] Выбор кастомных шрифтов в форме генерации (список из data/fonts/ добавляется к стандартным)
- [x] Экспорт многофайлового сайта в ZIP (`GET /api/projects/{id}/download-zip`, кнопка «🗜️ Скачать .zip»)

### 10.6 Опционально (не запланировано на MVP)
- [ ] Подключение реальных БД к сгенерированным сайтам (сейчас — JS-данные)

---

## Фаза 7: Tool calling для правок (быстрый путь) — 2026-08-23

### 11.1 Движок точечных правок
- [x] Сервис `app/services/patcher.py`: детерминированное применение операций LLM
- [x] Инструменты: `replace_text` (уникальный фрагмент), `set_css_property` (правило по селектору), `insert_before_end` (вставка перед закрывающим тегом), `delete_element` (удаление по tag#id/tag), `rewrite_full` (fallback-перезапись)
- [x] Гарантии: неуникальный `find` отклоняется, лимит 30 операций, отчёт с номером сбойной операции
- [x] Не тронутое остаётся байт-в-байт (нет дрейфа кода)

### 11.2 Интеграция в правки через чат
- [x] `edit_site()`: быстрый путь через tool calling → fallback на полный документ при любой ошибке
- [x] OpenAI-совместимая схема tools (`EDIT_TOOLS`) + системный промпт с приоритетом точечных операций
- [x] Парсинг tool_calls (строковые и объектные arguments), обработка невалидного JSON
- [x] Санитайзер: итоговый HTML проверяется целиком; `rewrite_full` — как обычная генерация

### 11.3 Тесты
- [x] `tests/test_patcher.py`: 12 тестов — все операции, отказы (неуникальность, неизвестный инструмент, битый rewrite_full), последовательное применение, индексация ошибок
- [x] Полный набор: 67 passed, 3 skipped

### 11.4 Эффект
- Простые правки («сделай шапку темнее») передают ~100–500 токенов вместо всего документа → ускорение в разы на больших сайтах
- Сложные правки автоматически уходят в `rewrite_full`/полный документ без второго запроса

---

## Фаза 8: Тарифные планы + vision-изображения — 2026-08-23

### 12.1 Vision-контент загруженных изображений
- [x] Настройки `vision_assets` (вкл/выкл) и `vision_assets_limit` (макс. изображений за запрос, по умолчанию 6)
- [x] `_asset_images_for()`: изображения проекта → base64 data URL (SVG пропускается — это код, не растр)
- [x] LLM получает картинки с подписями `[файл: <имя>]` + правило №14: размещать по СОДЕРЖИМОМУ (фото еды → меню, интерьер → «О нас»)
- [x] Тесты `tests/test_vision_assets.py`

### 12.2 Тарифные планы
- [x] Модель `User.plan` (free / standard / premium) + миграция `9c3d8e2f7a61`
- [x] Сервис `app/services/plans.py`: матрица возможностей; админам — премиум
- [x] **Free**: одностраничник по текстовому описанию, без изображений и анимаций
- [x] **Standard**: референсное изображение (vision), анимации и интерактив, загрузка изображений для страницы (ИИ размещает сам)
- [x] **Premium (Бета)**: всё из Standard + многостраничные сайты (заготовка), файловая система; в будущем — БД
- [x] Проверки тарифа в API: референс/multifile без нужного плана → 403
- [x] Промпт: free — статичный сайт без анимаций (правило №9)
- [x] UI формы: бейдж тарифа с описанием; недоступные блоки скрыты/показаны с 🔒
- [x] Переключатель «Полноценный сайт» убран — режим определяется тарифом автоматически

### 12.3 Следующие шаги (backlog)
- [ ] Многостраничность (premium): таблица Page, генерация нескольких HTML, роутинг превью, патчер по страницам
- [ ] Управление тарифами пользователей в админке
- [ ] Подключение БД к сгенерированным сайтам (premium)

---

## Фаза 9: Несколько LLM-провайдеров — 2026-08-23

### 13.1 Управление провайдерами
- [x] Таблица `llm_providers`: название, URL, API key, модель, таймауты, retries, max_tokens
- [x] Админка: создание, редактирование, удаление провайдеров
- [x] Переключатель `enabled` для включения/выключения провайдера
- [x] Приоритет провайдера: большее значение используется раньше

### 13.2 Отказоустойчивость
- [x] Основные запросы генерации используют включённые провайдеры по приоритету
- [x] При сетевой, HTTP или серверной ошибке автоматически пробуется следующий провайдер
- [x] Tool-calling правки также используют fallback
- [x] При отсутствии записей провайдеров используется резервная конфигурация из `.env`
- [x] Legacy-раздел `LLMConfig` удалён; резервная локальная LLM настраивается в `.env`
- [ ] В production: безопасное хранение/маскирование ключей и health-check провайдеров

---

## Фаза 10: Health-check LLM и премиум-демо — 2026-08-24

### 14.1 Индикатор «AI online» с реальной проверкой
- [x] Сервис `app/services/llm_health.py`: проверка через `GET /models` — без расхода токенов
- [x] Фоновая задача в lifespan: опрос при старте и раз в 1 минуту (не на клиенте)
- [x] Публичный эндпоинт `GET /api/llm-status` отдаёт кэш мгновенно
- [x] Индикатор в навбаре: 🟢 `AI online · <модель>` / 🔴 `AI offline` / 🟡 проверяется

### 14.2 Премиум-демонстрации (перезаписаны)
- [x] **Визитка «СТРОЙМИР»** (строительные материалы): индустриальный тёмный стиль, Oswald, «строительная» лента, бегущая строка цен, scroll-reveal, карточки категорий с неоновой подсветкой
- [x] **Лендинг «LUMIÈRE»** (онлайн-магазин света): светлый премиум, Fraunces + Manrope, рабочая корзина-счётчик с анимацией, marquee-лента, тёмная секция преимуществ
- [x] **Портфолио «max.volkov»** (3D-дизайнер): футуризм, Space Grotesk + JetBrains Mono, перспективная сетка на фоне, 3D-hover карточек проектов, анимированные скилл-бары, неоновые градиенты
- [x] Placeholder-изображения с пояснением «загружается в режиме Полноценный сайт» — пользователь видит, куда встанут его фото
- [x] Все демо проходят санитайзер проекта (только Google Fonts CDN)
